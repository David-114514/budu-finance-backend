spring:
  application:
    name: budu-finance-backend
  datasource:
    url: jdbc:mysql://localhost:3306/budu_finance_db?useSSL=false&serverTimezone=Asia/Hong_Kong&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

# ???? CORS????????
app:
  cors:
    allowed-origins: http://localhost:5173